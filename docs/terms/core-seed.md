# Term: Core Seed

Status: Active

The core seed is the reusable starting structure for an Aura project.

It contains development discipline and low-risk utilities. It should not contain a domain model, a persistence model, product-specific UI, or large reference datasets until a project explicitly needs them.

