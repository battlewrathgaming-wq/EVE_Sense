# Completed Workspace Handshakes

This folder stores batch-archived milestone handshakes.

During active milestone work, handshake files remain in `workspace/`.

When Overseer accepts milestone completion, move related handshakes into:

```text
workspace/complete/milestone-XX/
```

Do not archive individual handshakes during normal active work unless the user explicitly asks.
